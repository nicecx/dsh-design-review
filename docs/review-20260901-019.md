# 20260901-019 修订重提：方案 A v4（isSkillDoc 正则/T7 一致化 + S 系列命名）

- requestId: 20260901-019（修订 20260901-018 changes-requested 后重提）
- 类型: design（skill 开发路径关卡扩展 v4）

## 复用评估

- 本地 CAPABILITY-INDEX：dsh-design-review 插件自身扩展
- 外部（awesome）：无对应条目；GitHub：无直接同型方案
- 无复用：识别/门禁分支为新逻辑

## 方案（018 意见逐条落实）

### 1. [阻塞] isSkillDoc 正则与 T7 一致化 → 采纳修法 (a)
**落实**：
- **正则保持** `/[\/\\]skills[\/\\]/`（严格：skills 前必须有分隔符——生产实际中 write/edit 工具传绝对路径必含 `/skills/`，且防 `myskills/` 类误匹配）
- **T7 输入改为 `a/skills/x/y.skill.md`**（与 T6 `a/SKILL.md` 风格一致，贴合生产）
- 规则 B 表述同步：「路径含 `/skills/`（带分隔符）且文件名以 `.skill.md` 结尾」
- 三者（表述/正则/T7）一致

### 2. [建议] T9 锻炼 /skills/ 路径判定
**落实**：T9 输入改 `a/skills/soft-skills.md` → 期望 false（防"文件名含 skill"宽松回归 + 验证后缀逻辑：`soft-skills.md` 不以 `.skill.md` 结尾）

### 3. [提示] 独立前缀 S1-S4
**落实**：skill 识别用例命名 **S1-S4**（S1 `isSkillDoc('a/SKILL.md')`=true、S2 `isSkillDoc('a/skills/x/y.skill.md')`=true、S3 `isSkillDoc('README.md')`=false、S4 `isSkillDoc('a/skills/soft-skills.md')`=false），与既有 T 系列（设计识别/模板章两组）不拥挤

## 其余（v3 已定，不变）

- `gateByType(type, content)` 接缝导出（design=复用+模板；skill=仅复用；lesson=豁免）
- auto-submit：isSkillDoc 优先 → type='skill'；否则 isDesignDoc → type='design'
- 不新增 checkSkillReuse（复用 checkReuseSection）
- 既有 T1-T5/T1-T3（模板）不删

## 实施清单（approved 后）

1. core.js：`isSkillDoc`（正则 `/[\/\\]skills[\/\\]/` + basename 判断）+ `gateByType` 导出
2. index.js：submit() 用 gateByType；auto-submit 按 isSkillDoc→isDesignDoc 定 type
3. test：S1-S4（isSkillDoc）+ I1-I4（gateByType）= 30 用例
4. 重启部署

## 接口与兼容性

submit type='skill' 透传（消费端不特殊处理）；design/lesson 行为不变。

## 安全

无新权限；fail-closed 沿用（009 lesson）；skill 拒绝 deliver 原因（不静默）。

## 测试方案

node --test：22 旧（不动）+ S1-S4 + I1-I4 = 30 用例。

## 变更文件

- dsh-design-review/src/core.js（isSkillDoc + gateByType）
- dsh-design-review/src/index.js（submit/auto-submit 改造）
- dsh-design-review/test/index.test.js（S1-S4 + I1-I4）

## 风险与回滚

风险：skill 误识别 → 规则收紧 + S3/S4 防回归；回滚：还原 isSkillDoc/gateByType（单函数）。
